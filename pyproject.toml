[project]
name = "colegio-sys"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask",
    "flask-sqlalchemy",
    "flask-bcrypt",
    "psycopg2-binary",
    "python-dotenv",
]
